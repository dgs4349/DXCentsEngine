#pragma once

#include "SoundEngine.h"
using namespace FlashBang;

class Effect
{

/*
	static Effect factory library

	Effects.Volume(min, max) 
		- Creates and adds to Sound effect as a parameter, that can later be Connected using Connect, Time, Lifetime, LFO

*/

public:
	Effect();
	~Effect();
};

